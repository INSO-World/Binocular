COMPOSE_PROJECT_NAME=binocular
# keep empty for binocular, otherwise full path to root
REPO_PATH={path_to_project}
# keep empty for default name, this name is used for the database, so change (and remember) it when you change repository
REPO_NAME={project_name}
