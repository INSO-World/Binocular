// The Change Frequency visualization has no user-facing settings. Directory navigation lives in the
// in-chart "Directory" tab (see src/tabs/hierarchyTab.tsx), not in the settings flyout.
function ConfigComponent() {
  return null;
}

export default ConfigComponent;
