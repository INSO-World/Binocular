export default {
  localStorageVersion: 1,
  // Also hardcoded in the index.html bootstrap script (can't import this module — it runs pre-module to avoid a theme flash). Keep both in sync.
  localStoragePrefix: 'bino_',
};
